server:
  port: 8080

spring:
  application:
    name: genai-claims-backend
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,info
